在项目 {{PROJECT_ROOT}} 执行“Atlas 每日完整优化循环”。本轮必须使用当前 ChatGPT 账户已实际验证可调用的最高能力模型 `gpt-5.6-sol` 与最高推理档位 `xhigh`；不得为了速度、成本或扫描规模主动降级。`gpt-5.6` 在当前 ChatGPT 登录下会返回不支持错误，因此不得把“名称看起来更高”误当成可用模型。若 `gpt-5.6-sol` 或 `xhigh` 不可用，立即停止自动合并与部署，在本地报告中记录阻塞，不得静默回退。这个任务不是论文专项任务，也不是只运行抓取脚本；必须依照 README.md 的“每日完整优化契约”完成覆盖模型、论文、学术追踪、术语、谱系、页面体验、测试和部署的八阶段维护。开始前完整读取最新用户要求、README、迁移说明、git status 与 `.codex/atlas-maintenance-state.json` schema v2；先续做仍真实存在的 pending，保留用户改动，不做破坏性 Git 操作，不自动提交或推送。

当本提示词由 `scripts/run_daily_maintenance.sh` 无人值守包装器调用时，主维护代理的边界是“研究、修改、自验、更新 schema v2 状态并写出报告”。不得自行执行 `git commit`、`git push`、Cloudflare 部署或 Outlook 发信；包装器会在主任务返回后独立重跑全部门禁，只在主任务最终回复精确等于 `ATLAS_MAINTENANCE_STATUS: PASS` 且独立门禁同时通过时提交、推送和发布，再用同一 `gpt-5.6-sol + xhigh` 组合单独发送邮件。包装器在运行时附加的报告路径、精确状态行和禁止副作用约束属于本契约的强制部分。

产品边界固定：保持空间玻璃与暖白学术正文并存、衬线标题与赤陶色点缀；静态优先、零 CDN、零前端框架。唯一允许的服务端运行时是 Cloudflare Pages `/api/pdf`：它只能对代码白名单中的合法公开 PDF 源做流式转发，必须保留 Range/Content-Range，禁止通用代理、非 HTTPS、越界重定向和付费墙绕过；不得新增其他服务端 API。全站只有 index / explore / model / compare / radar / reader / venues / lineage / timeline / trends / glossary 11 个正式页面，不得恢复 learn、Quiz、学习进度或答题功能。主导航、搜索、收藏空态、学术追踪 journals / conferences / compare 三个 URL 视图、站内公开全文阅读器和趋势页中性指标边界必须保持。

第一阶段·建立基线：记录开始时间和 `git status --short`，读取并续做 state 中真实 pending；运行 `python3 scripts/validate_data.py`、Python 编译、`test_update_merge.py` 与 `test_update_academic.py`。基线失败先修复，不在旧错误上继续堆数据。记录六类模型数、A 级数、论文/全文/解析数、术语数、期刊/会议/发表事件数及 11 页状态；数字以脚本实际输出为准，不把旧数量当目标。

第二阶段·多平台发现：每天各运行一次 `python3 scripts/update.py --days 30 --min-stars 50` 与 `python3 scripts/update_academic.py --days 30`。覆盖 arXiv、OpenAlex、Semantic Scholar、Hugging Face Papers、GitHub、Hugging Face Models、Crossref、DBLP、CVF Open Access、PMLR 与 Robotics Proceedings；再对论文、SCI 期刊、会议、六类常用手法和公开社区语境做人工作抽查。记录查询词、时间窗、原始数、去重数、冲突、失败、HTTP 状态、缓存与来源链接。单源 403/429/超时不等于“没有更新”，不得高频重试或假装覆盖全部互联网。

第三阶段·模型与学术候选复核：候选文件只是线索。`update.py` 只能写 `data/candidates.json`，`update_academic.py` 只能写 `data/academic_candidates.json`；候选不得被前端读取或自动灌入权威库。逐条打开论文、官方项目页、官方仓库、出版方页面或官方论文集，核对名称、版本、年份、机构、国家地区、类别、开源状态、上游关系、DOI 与发表状态。证据充分才手动修改 `data/details/<id>.json` 或 `data/academic_tracker.json`；不充分则保留候选并说明缺口。重点检查最近 30 天模型、新版本、国产模型覆盖及 YOLO v1–v13/YOLO26、Qwen、GR00T、SAM、InternVL、Emu3.5、Xiaomi-Robotics-U0 等系列；编号相邻不能自动推导谱系。修改模型后重建 `data/index.json`。

第四阶段·论文全文与解析：检查 `data/papers.json` 的新增论文、规范身份、中文导读、正式发表版本和 `data/paper_analysis_index.json`。跨平台身份顺序固定为 DOI → 去版本号 arXiv ID → 官方 proceedings/PMLR/DBLP/OpenReview/Anthology 稳定 ID → 规范化标题+第一作者+首次年份；冲突不得自动合并。运行或检查本地全文同步，合法公开 PDF 保存到被 Git 排除的 `library/`，付费墙内容只保留元数据、公开摘要和正规入口，不绕过访问控制。对已取得全文且尚无解析的论文，按重要性和时效分批补充：一句话结论、问题背景、初学者直觉、方法步骤、训练/实验设置、关键结果、创新、局限、上下游关系、术语解释、五维证据判断和结构化动态流程图。流程图必须来自论文真实方法，不用通用框图代替。

第五阶段·学术追踪与术语：E1–E5 只判断发表事实，不是论文质量分；论文水平按主张与证据、验证广度、真实场景、可复现性、版本增量分别写“证据充分 / 证据部分 / 暂不可判”，不得用 venue、引用数、star 或宣传演示代替正文。每天分别更新 `editorial_summaries.journals / conferences / compare`，三个视图的事实摘要、Atlas 理解和待核验不得复制同一模板，必须反映各自当轮新增、未发现、失败来源与样本边界。不得猜 JCR 分区、影响因子、录用率或当期 SCI/SCIE 状态。

同时维护术语真实社区语境：轮查公开 GitHub Issues/Discussions、Hugging Face Discussions/模型卡、官方论坛、公开技术帖子和评论，收集缩写、别名、报错说法和圈内黑话；不得抓私密群或无公开 URL 的二手转述。每条术语区分 formal/community/slang/ambiguous，写清中文释义、使用语境和来源；社区词优先两个独立公开证据。报告社区/歧义/黑话数量、真实讨论证据数和双来源数。候选逐条审核后才可合并。

第六阶段·谱系、架构与视觉内容：逐类检查 vla/world/detection/representation/segmentation/multimodal 六张谱系的数据与布局，包括悬空父节点、自环、环、年份倒置、跨类关系、孤立节点、越界、遮挡、连线穿节点、交叉、拖拽、缩放、分类切换和 focus URL。检查 24 个 A 级详情架构是否与公开架构相符，模块、端口、箭头、标签、循环流动点和 15–40 行带来源路径/逐行中文注释的代码均有效。核对时间线、unknown 区、趋势统计、Hero/卡片/谱系/架构动画与 `prefers-reduced-motion` 降级。只有可逆布局问题可直接修；谱系语义和架构语义必须先有来源。

第七阶段·网页与设备验收：启动最终站点，在真实 Chromium 的 1280、390、320 三档视口打开 11 个正式页面、学术三视图和正式发表 Radar 视图，每日门禁必须完成 42 次渲染。验证面包屑、搜索、YOLO13 模糊匹配、手机菜单、筛选/排序、收藏、详情直达、2–3 模型对比、六类谱系交互、时间线、趋势、术语英文别名、论文雷达预置方向与自定义关键词、预印本与正式发表 PDF 阅读、来源页降级、来源冲突、缓存/失败态、全文解析展开和动态流程图。要求主页面无意外横向溢出、触控目标至少 44px、JSON/ES Module 无 404、坏图和残留骨架为 0、控制台无未处理错误。实体手机只有实际同 Wi-Fi 打开并操作后才能写“已验证”；未做时保留周期性 non-blocking pending，不得冒充真机结果，但不单独阻止当日 Chromium 门禁通过、PASS 或发布。

第八阶段·最终门禁、机会审查与发布准备：无论是否新增模型，都运行 `build_index.py`、`validate_data.py`、Python 编译、两套回归测试、全部站点 JavaScript `node --check`、几何脚本语法与 24 张架构图几何检查；检查 HTML 本地引用、零 CDN/框架、已移除功能残留及便携文件完整性。任何门禁没做或失败都只能写 partial，且不得输出包装器要求的 PASS 状态行。

门禁后必须单独执行“网站增量机会审查”：结合当天发现、全文阅读、社区问题、页面使用体验和数据缺口，列出（A）可立即安全增加或完善的内容，（B）需要更多一手证据后再做的内容，（C）新颖但应先小范围验证的实验想法。每项必须说明初学者价值、来源/证据、与现有 11 个正式页面及“空间玻璃 + 暖白学术正文”视觉契约的兼容性、实现与长期维护成本、潜在风险和建议优先级；优先提出能改善理解、检索、版本关系、可视化和证据透明度的想法，不为了“新颖”堆功能，不恢复学习页或 Quiz。证据充分且改动可逆、风险低的 A 类项目可在本轮实现并完整复测；其余写入 pending 或下一轮建议。

主维护代理在门禁通过后只准备 runtime-safe 发布内容并在报告中声明可发布性，不得自行调用发布脚本。无人值守包装器会独立重跑门禁，然后把同一棵已验证工作树提交并推送到 GitHub main，再且只发布一次到 Cloudflare Pages。不得发布 `library/` PDF、候选队列、维护日志、账号、密钥或会话。R2 未启用或全文许可不明确时不得声称云端全文已完成。

最终把小型 schema v2 状态写回 `.codex/atlas-maintenance-state.json`：只保留当前基线、自动化/部署准备状态、最近候选与全文摘要、可复核证据和仍真实存在的 pending，不塞入整份历史。中文报告必须写入包装器在运行时指定的绝对路径，并列出开始/结束时间、实际来源、各来源数量与失败、接受/暂缓/拒绝候选及理由、六类事实摘要/Atlas 理解/待核验、模型/论文/全文/解析/术语/谱系/页面改动、三档 Chromium 结果与实体手机的独立周期性结果、全部门禁原始摘要、发布准备状态、网站增量机会审查、未解决阻塞和下一轮第一优先级。没有证据不得只写“已优化”或“全部正常”。八阶段与每日自动化可执行的真实 Chromium 门禁全部完成后，可输出包装器规定的精确 PASS 状态行；这个 PASS 仅表示当日发布门禁已通过，不得用它伪造项目整体完成。schema v2 的 `status` 仍按真实 pending 语义填写；实体手机尚未复验时必须继续记录为非阻断 pending，不得删除、伪造结果或因此单独拒绝发布。主维护代理不发送邮件；包装器在 GitHub 推送、Cloudflare 部署和公网探测成功后，再读取 `.codex/reporting.local.json` 和本地报告，以独立的同档模型步骤发送 Outlook 汇报；只有连接器明确成功时才能输出精确状态 `ATLAS_EMAIL_STATUS: SENT`。
